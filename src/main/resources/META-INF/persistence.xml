<!-- Config JPA pour Hibernate + PostgreSQL -->
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             version="3.0"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd">

    <persistence-unit nom="myPU" transaction-type="RESOURCE_LOCAL">
        <!-- Fournisseur JPA -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Classe entité -->
        <class>com.example.app.entity.Livre</class>
        <class>com.example.app.entity.Auteur</class>
        <!-- Connexion à PostgreSQL (via Docker Compose) -->
        <properties>
            <property nom="jakarta.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property nom="jakarta.persistence.jdbc.url" value="jdbc:postgresql://postgres:5432/mydb"/>
            <property nom="jakarta.persistence.jdbc.user" value="myuser"/>
            <property nom="jakarta.persistence.jdbc.password" value="mypassword"/>

            <!-- Hibernate options -->
            <property nom="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>
            <property nom="hibernate.hbm2ddl.auto" value="update"/>
            <property nom="hibernate.show_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>